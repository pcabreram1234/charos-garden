const showModal = () => {
  true ? false : true;
};

const handleOk = () => {
  return false;
};

const handleCancel = () => {
  return false;
};

export default { showModal, handleOk, handleCancel };
